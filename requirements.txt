# self-improving-system-builder — pinned production dependencies
# Generated: 2026-07-04
# Python >= 3.11 required
# Install: pip install -r requirements.txt

# HTTP client (model_caller.py uses stdlib urllib, but conductor_shell uses httpx)
httpx==0.27.0

# Config / env
python-dotenv==1.2.2

# Retry / backoff
tenacity==8.3.0

# Structured logging
rich==13.7.1

# JSON schema validation
jsonschema==4.22.0

# WebSocket client (brain_client.py → the-brain MCP)
websockets==12.0
